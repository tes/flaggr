'use strict';

var expect = require('expect.js');

describe('Redis Adapter', function() {
  it('should not make jslint mad while is being build', function(done) {
    expect(true).to.be(true);
    done();
  });
});